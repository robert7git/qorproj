import React from 'react'
import LoadingStyle from './Loading.module.scss'

export const LoadingTiny = props => {
  return <div className={`tc ${LoadingStyle.LoadingTiny}`}>loading...</div>
}

export default LoadingTiny
